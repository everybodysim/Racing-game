# Template custom block pack

Replace placeholder files with real assets:

- `blocks/<block-id>/model.obj` or `.glb/.gltf`
- `textures/<texture-id>/albedo.png` (optional)

Then duplicate this folder to make additional packs and add its manifest path to `custom-block-packs/index.json`.
